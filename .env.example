# Frontend environment variables
# Base URL for the Express backend API.
# If you use the Vite dev proxy (vite.config.js /api -> localhost:5000),
# you can leave this as /api or omit it. Setting it explicitly is also fine.
VITE_API_URL=http://localhost:5000/api
